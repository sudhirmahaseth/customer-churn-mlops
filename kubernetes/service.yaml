apiVersion: v1
kind: Service
metadata:
  name: churn-api-service
  namespace: default   # 👈 यह अब सही जगह पर (metadata के अंदर) आ गया है
spec:
  type: NodePort
  selector:
    app: churn-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30007